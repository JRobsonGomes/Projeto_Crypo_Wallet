<!DOCTYPE html>
<html>
  <head>
    <title>CrypoWallet</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body data-flash-messages="<%= JSON.dump(flash.to_a) %>">
    <div class="container-fluid">
      <ul class="nav justify-content-end">
      <li class="nav-item">
          <%= link_to 'HOME', root_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Português', params.merge(locale: 'pt-BR').permit(:locale), class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to 'English', params.merge(locale: 'en').permit(:locale), class:"nav-link" %>
        </li>
      </ul> 
      <%= yield %> <%# index.html.erb, edit.html.erb, ...%>
    </div>
  </body>
</html>
